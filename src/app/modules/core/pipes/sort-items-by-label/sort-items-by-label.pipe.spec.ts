import { SortItemsByLabelPipe } from './sort-items-by-label.pipe';

describe('SortItemsByLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SortItemsByLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
